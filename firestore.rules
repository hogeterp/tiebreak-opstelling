rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TIJDELIJKE REGELS: de app heeft nog geen inlogfunctie.
    // Hierdoor kan iedereen met de website-link lezen en schrijven.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
