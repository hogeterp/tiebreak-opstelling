rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tijdelijk voor de besloten groep. Voeg later Firebase Authentication toe.
    match /{document=**} { allow read, write: if true; }
  }
}
